console.log('booleans');

// true or false, used for logic statements (if), manually set or calculated
const age = 18;
const ofAge = age > 19;
console.log(ofAge);

// equality
// one equals (=) is used for assignment - set and update

// almost always use triple equals for assessing value (===), checks value of thing on left and right side are same, and type are the same

// double equals (==) does not assess type, only value
